# Dashboard Contracts

This folder documents contracts for dashboard consumers.

- Operations dashboard should consume Decision API payloads.
- Executive KPI dashboard should consume aggregated outcomes from gold + feedback.
- Field workflow should surface recommended actions with evidence package.
